# Dataset config for YOLOv8n drone fine-tuning
# ----------------------------------------------
# Single-class drone detector.
# Labels use class index 0 = drone.

train: ../datasets/drone_detect/images/train
val:   ../datasets/drone_detect/images/val

nc: 1

names: [drone]
